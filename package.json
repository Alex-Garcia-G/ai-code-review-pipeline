{
  "name": "ai-code-review-pipeline",
  "version": "1.0.0",
  "description": "Multi-agent AI system that reviews pull requests like a senior engineer",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --env-file=.env --watch src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
